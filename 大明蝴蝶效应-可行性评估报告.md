# 《大明：蝴蝶效应》可行性评估报告

---

## 1. 执行摘要

《大明：蝴蝶效应》以"拖拽+连线"直觉操作替代传统菜单式策略交互，将AI定位为后台蝴蝶效应模拟引擎而非前端对话角色，设计理念清晰且差异化明显。评估结论：**交互设计与AI模拟层设计成熟度较高，系统架构风险可控但需警惕数值雪崩，视觉沉浸层为最大资源风险点**。建议原型阶段聚焦"拖拽银两到辽东→军队视觉反馈→1个AI事件气泡生成"单一闭环验证核心体验。若≤5人团队以Unity 2022 LTS+开发，12个月内可完成可玩原型。

---

## 2. 可行性评分矩阵

### 2.1 交互设计

| 指标 | 评分(1-5) | 评分理由 |
|------|-----------|---------|
| 拖拽精准度 | 4 | UGUI DragHandler可满足；但地图缩放后对小型节点的命中判定需额外调优射线检测半径，属于可解决的技术微调 |
| 连线反馈 | 3 | UGUI LineRenderer可绘制连线，但"连线过程中实时显示连接方向"与"朝臣头像→区域节点一对多"的视觉路由（避免交叉重叠）复杂度较高，需自定义贝塞尔曲线布线算法 |
| 点击响应 | 5 | 标准UGUI Button/EventTrigger体系，无技术难题 |
| 手势容错 | 3 | 拖拽与滚轮缩放、右键拖拽地图的输入冲突需专门处理；移动端触屏（若扩展）需额外适配双指缩放与单指拖拽的区分 |
| 学习成本 | 4 | 拖拽/连线为直觉操作，但游戏深度（蝴蝶效应的跨域因果关系）可能导致认知负担——建议沙盘内嵌"因果预览线"（操作前高亮潜在影响链） |
| **综合均分** | **3.8** | 核心交互可行，连线视觉路由与手势冲突为原型阶段重点验证项 |

### 2.2 系统架构

| 指标 | 评分(1-5) | 评分理由 |
|------|-----------|---------|
| 组件复用性 | 5 | "资源球/连线/事件气泡"均为独立Prefab，符合GameObject-Component模式；朝臣头像可抽象为PersonCard组件，区域节点可抽象为RegionNode组件——复用性设计优秀 |
| 耦合度 | 3 | 蝴蝶效应天然导致高耦合：资源变更→军队战力→民心→叛乱→外部势力链路的依赖关系错综。需引入事件总线解耦，否则后期维护成本指数级增长 |
| 扩展性 | 4 | 事件驱动架构下新增事件类型/资源种类/人物属性无需修改核心循环，扩展性良好；但可视化层（疆域图新元素）需对应新增渲染层，增一分算一分 |
| 状态一致性 | 3 | 前端即时反馈（拖拽即变）与后端AI异步响应之间可能存在"短暂不一致"窗口——玩家看到军队变强，2秒后AI告知实际缺粮。需设计"乐观更新+回滚"或"预览模式"策略 |
| 性能开销 | 3 | 疆域图千级节点+商路动画+饥民扩散+烽火变色均在<100ms刷新，在Canvas（非World Space）渲染模式下压力较大，需验证Draw Call瓶颈 |
| **综合均分** | **3.6** | 架构模式正确，但蝴蝶效应耦合与状态一致性是核心挑战；事件总线设计将被后文详细展开 |

### 2.3 AI模拟

| 指标 | 评分(1-5) | 评分理由 |
|------|-----------|---------|
| 演算延迟 | 2 | HTTP API异步调用延迟不可控（通常1-5s），与<3s响应的设计目标存在差距。需引入"预生成事件池+实时请求降级为本地规则"的混合策略 |
| 事件多样性 | 4 | AI基于当前世界状态动态生成而非固定剧本，多样性天然有保证；但边界case（如所有地区都太平无事）时需确保AI能生成"内部矛盾"类型事件避免空洞期 |
| 人物行为合理性 | 3 | "朝臣自主行动"需AI持续维护所有NPC状态（8-12人），若每次都全量传入状态→token消耗大且延迟高；需设计"仅推送变更+温度参数控制行为频率" |
| 数值连锁准确性 | 2 | 明末经济/军事/民心多变量连锁对AI纯文本推理的准确性存疑——AI可能生成"加辽饷→东林党忠诚提升"这种不符合系统逻辑的输出。需后置校验层拦截 |
| 可调试性 | 2 | AI黑盒推演难以复现Bug：无法"回放"某个决策链并稳定获得相同蝴蝶效应。需设计状态快照+种子系统，使AI输出在给定seed下可复现 |
| **综合均分** | **2.6** | 本维度为项目最大不确定性来源。AI作蝴蝶效应引擎的设计大胆但风险集中——建议原型阶段先用脚本规则引擎验证核心循环，AI作为"事件生成器"而非"完整模拟器"逐步接入 |

### 2.4 视觉沉浸

| 指标 | 评分(1-5) | 评分理由 |
|------|-----------|---------|
| 疆域动态刷新 | 3 | Canvas下以Image/ScrollRect承载疆域图，千级节点+部分动画元素在移动/缩放时的重绘开销需实测。Tilemap方案更适合静态地形但不利于UI交互 |
| 四季昼夜过渡 | 2 | 若以Canvas实现，需多层半透明遮罩+Shader调色，技术可行但叠加"商路马车/饥民扩散"等多动画层后，Canvas合批(batch)可能被频繁打断 |
| UI动效流畅度 | 4 | DOTween/LeanTween可满足连线动画、资源球飞入飞出、气泡弹出等需求，成熟方案 |
| 信息层次 | 4 | "总览界面→沙盘主界面→事件气泡→详情弹窗"的信息层级设计合理；关系网的节点-边图布局用UGUI实现可行但需注意大型关系网的性能 |
| 氛围塑造 | 3 | 动态烽火/饥民扩散/势力范围变色等设计可传递历史沉浸感，但美术资源（四季贴图、军队模型动画、商路马车Sprite序列）的产出量是一大风险——5人团队需优先制定最小美术范围 |
| **综合均分** | **3.2** | 设计方向正确，但Canvas渲染瓶颈与美术产出量是两大资源风险。建议原型阶段使用占位美术(几何图形+纯色)优先验证交互逻辑 |

### 评分总览

| 维度 | 综合均分 |
|------|---------|
| 交互设计 | 3.8 / 5.0 |
| 系统架构 | 3.6 / 5.0 |
| AI模拟 | 2.6 / 5.0 |
| 视觉沉浸 | 3.2 / 5.0 |
| **项目综合** | **3.3 / 5.0** |

> 项目可行性中上。最大风险集中在AI模拟层的延迟、准确性与可调试性。原型阶段需优先验证Top 3风险（见第3节）。

---

## 3. 风险分析

### 技术风险（优先级最高）

**[R1] AI响应延迟超限导致玩法断层** | 概率：高 | 影响：高
缓解方案：(a) 实现"事件池预生成"机制——每隔若干逻辑帧，后台静默请求AI生成3-5个待触发事件缓存；(b) 玩家操作后的即时反馈使用本地规则引擎（先展示确定性后果的预览），AI结果返回后异步更新；(c) 如3秒内无响应，降级为预设事件模板+当前状态填充变量。

**[R2] Canvas渲染千级交互节点时Draw Call爆炸** | 概率：高 | 影响：中
缓解方案：(a) 沙盘地图使用RenderTexture+摄像机方案而非纯UGUI，将地形/边界/动态元素渲染至一张纹理；(b) UGUI仅承载交互层（资源球/连线/气泡），节点超过可见区域时采用对象池+视锥剔除；(c) 商路马车、饥民扩散等群体动画使用GPU Instancing或VFX Graph而非独立GameObject。

**[R3) 蝴蝶效应数值雪崩——微小输入导致全系统崩溃** | 概率：中 | 影响：高
缓解方案：(a) 设计"影响衰减系数"：每级因果传播乘以衰减因子(如0.6)，使连锁影响的边际效应递减；(b) 为关键数值设置上下限阈值(如民心[0,100]、国库[-∞, +∞]但不为负时触发破产事件)；(c) 引入"系统惯性"机制——大系统的反应有延迟，不会因单一决策瞬间翻转。

**[R4] UGUI连线在众多朝臣-区域交叉时视觉混乱不可读** | 概率：中 | 影响：中
缓解方案：(a) 朝臣头像栏采用"筛选Tab"（阁臣/将领/地方官）减少同屏连线数量；(b) 连线使用贝塞尔曲线自动绕行算法(如A*在连线空间寻路)；(c) 选中某一区域节点时，仅显示与此区域相关的连线，其余半透明。

**[R5] AI人物行为不合理输出（OOC）破坏历史沉浸** | 概率：中 | 影响：中
缓解方案：(a) 每个NPC内置"行为边界表"(例如左良玉: 不可主动勤王、不可降低自身兵权)；(b) AI输出后经本地规则校验层拦截不合理行为(如"吴三桂主动送粮给李自成")；(c) 拦截后以"该角色暂无特殊动向"替代，不暴露AI拒绝信息。

### 系统风险

**[R6] 状态一致性问题：前端乐观更新与AI后台计算结果出现分歧** | 概率：中 | 影响：高
缓解方案：(a) 明确区分"确定性操作"(拖拽资源=即时生效)与"非确定性操作"(AI评估的连锁影响=异步更新)；(b) 确定性操作立即更新UI+写入本地状态；非确定性结果返回时做"delta合并"而非全量覆盖；(c) 分叉过大时触发"修订叙事"——如"前线传来回报，此前的军需调拨因道路毁坏未按时抵达"来合理化数值回滚。

**[R7] 关系网状态爆炸——8个NPC两两关系的存储与更新复杂度为O(n²)** | 概率：中 | 影响：低
缓解方案：(a) 仅存储每个NPC对玩家的关系+NPC间派系归属(阉党/东林/中立)；(b) NPC间关系在需要时由派系归属+近期事件动态推算，不持久化存储全量对；(c) 此方案将存储复杂度从O(n²)降至O(n)。

**[R8] 循环依赖：加辽饷→国库减→加税→流民多→需更多军饷镇压→再加税** | 概率：低 | 影响：中
缓解方案：(a) 识别"恶行循环"模式并在3轮后由AI生成"强制中断事件"(如"户部尚书以死进谏，暂停加税")；(b) 或将此循环作为玩法本身，让玩家体验明末系统性困境——但在崩溃前给出明确警示("国势曲线已进入下行螺旋")。

**[R9] 存档/读档与AI种子复现的矛盾** | 概率：中 | 影响：中
缓解方案：(a) 存档时记录：(i) 完整游戏状态JSON (ii) AI请求seed (iii) 已生成事件池的快照；(b) 读档后先加载快照确认无差异，再以seed请求AI恢复"未来可能事件"；(c) 不允许在AI返回前再次存档(加锁)。

### 内容风险

**[R10] 美术资源（四季贴图/马车动画/饥民模型/UI动效）产出远超5人团队能力** | 概率：高 | 影响：中
缓解方案：(a) 原型及EA阶段使用几何图形+纯色+图标字体的"极简明制美学"风格；(b) 商路用移动的细线代替马车动画，饥民用逐渐扩散的点阵代替模型，烽火用颜色渐变代替粒子特效；(c) 正式版美术资源采用外包+Asset Store采购策略。

**[R11] 历史内容准确性：AI可能生成不符合明末史实的推演结果** | 概率：中 | 影响：低
缓解方案：(a) 在AI prompt中嵌入关键历史锚点约束(如"李自成破京不可阻止但可延迟")；(b) 对渲染到玩家界面的最终文本进行关键词敏感词过滤；(c) 游戏开局声明"此为架空历史演绎"，降低考证预期。

> **Top 3 必验风险（原型阶段）**: R1(AI延迟) > R3(数值雪崩) > R2(渲染瓶颈) —— 这三项直接决定核心玩法能否成立。

---

## 4. 原型阶段技术规格

### 4.1 核心场景结构

```
Assets/Scenes/
├── Bootstrap.unity          # 启动场景：加载管理器、初始化事件系统
├── Sandbox.unity            # 主沙盘场景
│   ├── Main Camera          # 正交摄像机，渲染沙盘层
│   ├── Canvas (Screen Space - Overlay)
│   │   ├── MapLayer         # 疆域图（RenderTexture显示）
│   │   ├── ResourceBalls    # 资源球容器（国库/粮仓/威望/精力）
│   │   ├── MinisterPanel    # 朝臣头像栏（底部/侧边可折叠）
│   │   ├── PolicyScroll     # 政策卷轴悬浮按钮+弹出面板
│   │   ├── EventBubbles     # 动态事件气泡层
│   │   └── OverlayUI        # 国势曲线/关系网/编年史切换面板
│   └── EventSystem          # UGUI EventSystem + 自定义InputManager
└── Managers (DontDestroyOnLoad)
    ├── GameManager          # 游戏状态机 + 主循环
    ├── EventBus             # 全局事件总线
    ├── AIClient             # HTTP异步调用管理
    ├── SaveLoadManager      # 存档序列化
    └── AudioManager         # 环境音+UI音效
```

### 4.2 关键组件定义

| 组件 | 继承 | 职责 |
|------|------|------|
| `ResourceBall` | MonoBehaviour, IBeginDragHandler, IDragHandler, IEndDragHandler | 处理拖拽起止、拖拽中射线检测目标区域节点、拖放成功后触发`ResourceTransferEvent` |
| `RegionNode` | MonoBehaviour, IDropHandler | 接收拖放资源、连线锚点、显示区域数值(军队/民心/粮产)、响应连线鼠标进入/离开高亮 |
| `ConnectionLine` | MonoBehaviour | 使用LineRenderer或自定义UGUI Image绘制贝塞尔连线、支持颜色/粗细随关系值变化、悬停显示信息 |
| `MinisterCard` | MonoBehaviour, IBeginDragHandler(连线模式) | 朝臣头像+简要档案、拖拽生成临时连线、连线模式下显示可连接区域高亮 |
| `EventBubble` | MonoBehaviour, IPointerClickHandler | 浮动提示气泡、根据事件类型(危机/机会/朝堂)显示不同颜色图标、点击打开事件处理面板 |
| `SandboxCameraController` | MonoBehaviour | 鼠标拖拽平移地图、滚轮缩放、边界限位、双击聚焦区域节点 |
| `ButterflyEffectResolver` | MonoBehaviour (非UI) | 接收AI返回的影响链数据、解析后分发至各组件更新数值/动画/视觉反馈、管理"乐观更新-回滚"状态 |
| `StateManager` | MonoBehaviour (非UI) | 维护全局状态快照、通过EventBus与UI层通信、生成存档JSON、加载存档并广播恢复事件 |

### 4.3 数据流架构

```
┌─────────────────────────────────────────────────────────┐
│  EventBus (全局事件系统)                                  │
│                                                          │
│  ┌─────────────────┐  ┌──────────────┐  ┌─────────────┐ │
│  │ ResourceChannel  │  │PersonChannel │  │MapChannel   │ │
│  │                  │  │              │  │             │ │
│  │ ResourceTransfer │  │PersonAssigned│  │RegionUpdate │ │
│  │ ResourceDepleted │  │LoyaltyShift  │  │BorderChange │ │
│  │ TaxPolicyChanged │  │MinisterLeft  │  │RefugeeFlow  │ │
│  └────────┬────────┘  └──────┬───────┘  └──────┬──────┘ │
└───────────┼──────────────────┼─────────────────┼────────┘
            │                  │                  │
     ┌──────▼──────┐    ┌──────▼──────┐   ┌──────▼──────┐
     │ ResourceBall │    │ MinisterCard│   │ RegionNode  │
     │ (UI更新)     │    │ (头像/连线) │   │ (颜色/图标) │
     └─────────────┘    └─────────────┘   └─────────────┘
            │                  │                  │
     ┌──────▼──────────────────▼──────────────────▼──────┐
     │         StateManager (状态快照)                    │
     │  ┌─────────────────────────────────────────────┐  │
     │  │ { resources: {}, persons: {}, regions: {} }  │  │
     │  └─────────────────────────────────────────────┘  │
     │              │                          │          │
     │      ┌───────▼───────┐          ┌──────▼──────┐   │
     │      │ AIClient       │          │ SaveLoad    │   │
     │      │ (异步调用)      │          │ Manager     │   │
     │      └───────┬───────┘          └─────────────┘   │
     │              │                                     │
     │      ┌───────▼───────────┐                         │
     │      │ EventPool         │                         │
     │      │ (预生成事件缓存)    │                         │
     │      └───────────────────┘                         │
     └───────────────────────────────────────────────────┘
```

**三个事件通道职责：**

- **ResourceChannel**: 资源变更事件(国库+-、粮仓+-、威望+-)。ResourceBall拖放完毕→发布事件→StateManager更新→RegionNode/其他ResourceBall更新显示。
- **PersonChannel**: 人物状态变更(任命/忠诚度变化/自主行为)。MinisterCard连线完成或AI返回NPC自发行为→发布事件→更新关系网图+触发相关事件气泡。
- **MapChannel**: 疆域/视觉变更(势力范围变化/饥民流动/烽火状态)。AI返回连锁影响或时间推进→发布事件→沙盘渲染层更新(颜色/图标/动画)。

### 4.4 沙盘渲染方案

#### Canvas vs Tilemap 对比

| 维度 | Canvas (UGUI Image + ScrollRect) | Tilemap (World Space + Camera) |
|------|----------------------------------|-------------------------------|
| 交互实现 | UGUI原生支持拖拽/点击/射线检测，连线层直接叠加 | 需额外处理3D→2D屏幕坐标转换，交互射线检测需手动实现 |
| 动态元素 | Image组件变色/位移容易，动画用DOTween | Tile/Sprite动态替换灵活，群体动画可用GPU Instancing |
| 性能（千级节点） | Draw Call随Image数量线性增长，Canvas重建(SetPass)可能成为瓶颈 | Tilemap合批天然高效，但叠加多层Tilemap后复杂度增加 |
| 缩放/平移 | ScrollRect天然支持，但缩放时所有Image重绘 | 摄像机正交大小调整原生支持，平移通过摄像机位置 |
| 美术资源 | 需要大量UI Sprite切片 | Tile Palette资源制作效率高，适合地形 |
| 推荐 | UI层(MapLayer作为RenderTexture展示) | 底层地形渲染 |

**推荐方案（混合）**：地形/势力范围底层使用Tilemap（渲染到RenderTexture），再通过RawImage在Canvas中显示。交互层（区域节点、连线、气泡）在Canvas中叠加。此方案兼顾渲染性能与交互便利性。

**性能预估（针对<100ms刷新底线）**：
- Tilemap渲染大明疆域（约2000-3000 Tile）：<5ms（GPU合批）
- Canvas交互层（约50-100个活动GameObject）：<10ms
- 连线重绘（最多20条贝塞尔曲线）：<5ms
- 数据更新计算（资源连锁+势力范围重算）：<10ms
- **总计预估**：<30ms，远低于100ms目标

### 4.5 AI集成方案

```
┌────────────────────────────────────────────────────┐
│                   AIClient                          │
│                                                     │
│  ┌──────────────┐   ┌─────────────────────────┐    │
│  │ RequestQueue │   │ EventPool (预生成缓存)     │    │
│  │ (优先级队列)  │   │ - 容量: 5-10个待触发事件   │    │
│  │              │   │ - 后台静默补充              │    │
│  └──────┬───────┘   └───────────┬─────────────┘    │
│         │                       │                   │
│  ┌──────▼───────────────────────▼───────────────┐  │
│  │            AI API (HTTP Async)                │  │
│  │  ┌─────────────────────────────────────────┐ │  │
│  │  │ Request: { snapshot: State, action: {} }  │ │  │
│  │  │ Response: { events: [], npc_actions: [] } │ │  │
│  │  └─────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────┘  │
│                                                     │
│  降级策略 (API超时/不可用):                           │
│  ┌─────────────────────────────────────────────┐   │
│  │ LocalRuleEngine (脚本规则引擎)                │   │
│  │ - 预设事件模板库(30-50个基础事件)              │   │
│  │ - 参数填充: 模板变量由当前状态数值替代          │   │
│  │ - NPC行为: 基于规则的状态机(无自主"创意"行为)   │   │
│  └─────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────┘
```

**调用策略（推荐"事件触发+状态缓存"而非"每秒全量"）**：

| 方案 | API调用频率 | 月成本估算(按GPT-4o) | 延迟体验 | 实现复杂度 |
|------|------------|---------------------|---------|-----------|
| 每秒全量调用 | 60次/分钟 | 极高(~$2000+/月) | 差（累积延迟） | 低 |
| **事件触发+缓存(推荐)** | 5-15次/分钟 | 可控(~$200-500/月) | 良好（异步+预生成） | 中 |
| 纯脚本规则(降级) | 0次 | 极低 | 可玩但深度受损 | 高（需设计完整规则集） |

**推荐方案细节**：
1. 玩家每次拖拽/连线/点击完成后，收集操作→打包状态快照→入队请求
2. 同时从EventPool中取出一个预生成事件立即展示（若无缓存，使用本地规则快速生成）
3. AI请求返回后，替换/补充EventPool中的事件
4. 关键操作（如南迁决定、吴三桂立场）触发同步等待（<3s），非关键操作完全异步

---

## 5. 3阶段开发路线图

### 阶段一：核心交互原型（第1-3月）

**阶段目标**：验证"拖拽资源+视觉反馈"核心玩法闭环的可行性，不接入AI。

**核心交付物**：
1. Unity空项目搭建，完成Bootstrap+Sandbox场景结构
2. `ResourceBall`拖拽到`RegionNode`的完整交互（拖拽起→移动跟随→放置检测→数值更新反馈）
3. 沙盘疆域地图：使用占位Tilemap绘制大明疆域+大顺/大西/后金势力范围
4. 3个区域节点（辽东/开封/宁远）可接收银两/粮草拖放，触发视觉变化（颜色/图标大小）
5. 本地脚本规则：银两→军队图标变化，粮草→流民图标变化

**技术里程碑**：
- ✅ UGUI Drag & Drop事件链在Canvas中稳定运行
- ✅ Tilemap+RenderTexture+Canvas混合渲染方案验证通过
- ✅ EventBus三通道事件框架跑通

**验收标准（可测试）**：
- [ ] 拖拽资源球到区域节点100次，成功触发率为100%（无漏触发/误触发）
- [ ] 区域节点接收资源后数值变化延迟<100ms（用Stopwatch测量UI更新间隔）
- [ ] 缩放/平移地图时拖拽操作不受干扰（缩放后拖拽偏移量需按scale factor修正）
- [ ] 同时存在3个资源球+5个区域节点时，帧率保持≥60fps

### 阶段二：AI接入与蝴蝶效应（第4-8月）

**阶段目标**：接入AI API，实现事件气泡动态生成与蝴蝶效应连锁反馈。

**核心交付物**：
1. `AIClient`完整实现：请求队列、EventPool、超时降级(LocalRuleEngine)
2. 事件气泡系统：AI生成的事件在沙盘上以浮动气泡呈现，点击可处理
3. 蝴蝶效应连锁反馈：拖拽银两到辽东→AI返回连锁结果→沙盘上多个区域同步更新（给辽东加饷→开封欠饷→开封出现叛乱气泡）
4. 朝臣连线系统：`MinisterCard`拖线到`RegionNode`完成任命，AI评估产生NPC关系/忠诚度变化
5. 状态快照+存档/读档：完整JSON序列化，含AI seed

**技术里程碑**：
- ✅ AI API延迟P95 < 3s（含EventPool预生成覆盖）
- ✅ 单次拖拽→连锁反馈完整周期<5s（本地预览<100ms + AI异步更新<5s）
- ✅ 存档/读档循环10次无状态丢失或差异

**验收标准（可测试）**：
- [ ] EventPool缓存始终≥3个待触发事件（30分钟连续游戏中任意时刻检查）
- [ ] AI返回的事件中≥80%通过本地逻辑校验（无"左良玉主动勤王"类OOC输出）
- [ ] 蝴蝶效应连锁链路在UI上的反馈路径可追溯（点击因果链中任一节点可高亮其上下游关联）
- [ ] 存档文件大小<5MB，读档恢复后所有数值与存档时一致（写自动化比对脚本）
- [ ] 断网时游戏不崩溃，降级为本地规则引擎继续运行，且提示"离线模式"

### 阶段三：视觉打磨与内容完善（第9-12月）

**阶段目标**：提升视觉沉浸感，完善历史氛围，进行平衡性调优。

**核心交付物**：
1. 四季/昼夜视觉系统：Tilemap底层着色器支持季节色调切换，Canvas遮罩层实现昼夜明暗
2. 国势曲线与关系网图：折线图+力导向图在UGUI中的自定义绘制
3. 编年史界面：自动生成的文本年表+事件缩略图
4. 音效系统：环境音（风声/雨声/朝堂嘈杂）、UI音效、区域警告音
5. 难度与平衡调优：数值系统的衰减系数/系统惯性参数经过≥50轮测试调整

**技术里程碑**：
- ✅ 四季切换完成时间<2s，过渡动画流畅
- ✅ 关系网图在20个NPC时渲染帧率≥30fps（此处可接受较低帧率，因为是静态面板）
- ✅ 完整游戏流程可玩时间≥2小时/局

**验收标准（可测试）**：
- [ ] 完整游玩一局（崇祯十七年二月→五月游戏内时间），无阻塞性Bug
- [ ] 5名外部测试玩家独立游玩后评分≥3.5/5（主观评价但可量化）
- [ ] 所有UI面板在1280×720与1920×1080分辨率下布局正常、无文字溢出或截断
- [ ] 退出游戏时状态自动存档，重新启动后可从存档继续
- [ ] AI事件不重复率≥70%（一局游戏中出现的事件气泡）

---

## 附录A：最小可行原型（MVP）定义

若团队资源/时间不足以支撑完整原型，建议将范围缩小至以下**单一闭环**：

> **"拖拽银两资源球至辽东区域节点 → 军队图标从'瘦弱'变为'健壮' → 3秒后在开封区域自动出现一个'欠饷'事件气泡"**

此闭环包含：拖拽交互 + 资源-数值映射 + 硬编码连锁反馈 + 事件气泡UI。
验证价值：**证实了"用手直接拖拽操作沙盘→看到因果连锁"的核心体验是否有趣**。

若此闭环在5人团队中无法在4周内完成，建议重新评估项目范围。

## 附录B：技术选型补充对比

### UGUI vs UI Toolkit（用于沙盘交互层）

| 维度 | UGUI | UI Toolkit (UI Elements) |
|------|------|--------------------------|
| 成熟度 | 成熟，文档/社区丰富 | 较新，部分功能(如世界空间)不够稳定 |
| 拖拽系统 | 内置IDragHandler等接口 | 需手动实现PointerManipulator |
| 连线绘制 | 无原生支持，需Image+自定义Mesh | 支持Vector API绘制，更灵活 |
| 性能(大量UI元素) | Canvas重建开销大 | 以保留模式渲染，重绘效率更高 |
| 5人团队适用性 | 学习成本低，Asset Store资源多 | 学习曲线较陡，可参考案例少 |

**结论**：推荐UGUI。原型阶段以快速验证为核心目标，UGUI的生态系统优势大于UI Toolkit的长期性能优势。后期若Canvas渲染成为瓶颈，可将地图层迁移至UI Toolkit的PanelSettings+RenderTexture方案，不影响核心交互逻辑。

---

> **报告版本**: v1.0 | **评估基准**: 2026年5月 | **目标平台**: PC (Windows)
>
> *"蝴蝶效应最迷人的地方不在于它有多复杂，而在于玩家能亲手触摸到那些涟漪。"*
